{
  "name": "gifwiddit-ruxton",
  "version": "0.4.1",
  "description": "Electro n Fool.",
  "dependencies": {
    "menubar": "*"
  },
  "devDependencies": {
    "electron-packager": "^9.0.0",
    "electron": "^1.7.5"
  },
  "main": "index.js",
  "scripts": {
    "build":
      "./node_modules/.bin/electron-packager . Gifwiddit --out build --platform=darwin --arch=x64 --version=1.3.3 --app-version=0.4.1 --cache=$HOME/.gifwiddit --asar --overwrite",
    "start": "./node_modules/.bin/electron --enable-sandbox ."
  }
}
